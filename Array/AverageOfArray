//Write a Domanda1 class that contains a main method, 
//in which you declare the array of integers (vector) V = [6,3,1,2,9] 
//and calculate the average of the array, printing it on the screen.

public class Domanda1 {

	public static void main(String[] args) {
		int[] V = {6, 3, 1, 2, 9};
		
		int sum = 0;
		for(int i = 0; i < V.length; i++) {
			sum = sum + V[i];
		}
		
		double average = (double) sum / V.length;
		System.out.println(average);
	}

}
